Знайти кількість слів у тексті\hypertarget{md_Markdown_remakelab64_autotoc_md22}{}\doxysubsubsection{1. функція count}\label{md_Markdown_remakelab64_autotoc_md22}
\begin{DoxyVerb}void count(char text[]){
    int size;
    int q = 0;
    while (text[q] != '\0'){     <- Визначає  розмір тексту
        q++;
        size = q;
    }
    size++;


   int chet = 0;

   for (int i = 0; i < size ; i++) {     <- визначає кількість слів у тексті
      if (text[i] == ' ') {
          for (int j = i; ; j++) {
              if (text[j] == ' ') {
              }
          else {
               chet++;
               i = j;
               break;
              }
          }
      }

  }
if (text[0] != ' '){              <- опрацьовує виключення, коли перша літера тексту - пробіл
    chet++;

}
if (text[size - 2] == ' '){      <- опрацьовує виключення, коли перша остання тексту - пробіл
    chet--;

}
    printf("%d\n", chet);
}
\end{DoxyVerb}
 \hypertarget{md_Markdown_remakelab64_autotoc_md23}{}\doxysubsubsection{2. main функція}\label{md_Markdown_remakelab64_autotoc_md23}
\begin{DoxyVerb}int main()
{

        char text[] = {  " g g g g g g 1 31 fd" };    <- текст
    count(text);                                  <- викликає функцію count
    return 0;
}
\end{DoxyVerb}
 \hypertarget{md_Markdown_remakelab64_autotoc_md24}{}\doxysubsubsection{3. блок-\/схема}\label{md_Markdown_remakelab64_autotoc_md24}
 